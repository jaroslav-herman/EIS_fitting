[project]
name = "eis-fitting"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "wepy",
]

[tool.uv.sources]
wepy = { path = "../wepy", editable = true }
